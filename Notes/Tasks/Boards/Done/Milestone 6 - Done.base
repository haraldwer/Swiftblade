views:
  - type: cards
    name: Table
    filters:
      and:
        - file.folder == "Tasks/Archive"
        - file.hasTag("M5")
    order:
      - file.name
    sort:
      - property: file.name
        direction: DESC
