views:
  - type: cards
    name: Table
    filters:
      and:
        - file.path.contains("Tasks/Backlog")
        - file.hasTag("editor")
    order:
      - file.name
    sort:
      - property: file.name
        direction: DESC
