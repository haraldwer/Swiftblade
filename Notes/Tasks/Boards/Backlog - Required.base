views:
  - type: cards
    name: Table
    filters:
      and:
        - file.path.contains("Tasks/Backlog")
        - or:
            - file.name.startsWith("0")
    order:
      - file.name
    sort:
      - property: file.name
        direction: DESC
