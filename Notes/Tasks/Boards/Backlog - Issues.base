views:
  - type: cards
    name: Table
    filters:
      and:
        - file.folder == "Tasks"
        - or:
            - file.name.startsWith("1")
    order:
      - file.name
    sort:
      - property: file.name
        direction: DESC
