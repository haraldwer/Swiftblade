views:
  - type: cards
    name: Table
    filters:
      and:
        - file.folder == "Tasks"
        - file.hasTag("M3")
    order:
      - file.name
    sort:
      - property: file.name
        direction: DESC
