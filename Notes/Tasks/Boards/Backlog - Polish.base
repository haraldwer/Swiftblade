views:
  - type: cards
    name: Table
    filters:
      and:
        - file.folder == "Tasks"
        - or:
            - file.name.startsWith("2")
    order:
      - file.name
    sort:
      - property: file.name
        direction: DESC
